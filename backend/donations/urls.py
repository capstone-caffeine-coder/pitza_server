from django.urls import path
from . import views

app_name = 'donations'

# urlpatterns = [
#     path('details/<int:request_id>/', views.detail, name='detail'),
#     path('request/', views.create, name='create'),
#     path('match/', views.match, name='match'),
#     path('select/', views.select_match, name='select_match'),
#     path('reject/', views.reject_match, name='reject_match'),
# ]
